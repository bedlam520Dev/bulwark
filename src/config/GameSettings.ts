/**
 * Game Settings for BULWARK
 * Centralized configuration for all tunable game parameters
 */

export const GameSettings = {
  debug: true,

  canvas: {
    width: 720,
    height: 1080,
  },
}

export default GameSettings
